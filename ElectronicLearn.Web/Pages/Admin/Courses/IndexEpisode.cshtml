@page
@model ElectronicLearn.Web.Pages.Admin.Courses.IndexEpisodeModel
@{
    ViewData["Title"] = $"بخش های دوره {Model.AdminEpisodeListViewModel.CourseName}";
}

<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                لیست قسمت ها
            </div>
            <!-- /.panel-heading -->
            <div class="panel-body">
                <div class="table-responsive">
                    <div id="dataTables-example_wrapper" class="dataTables_wrapper form-inline" role="grid">
                        @*<form>
                        <div class="row">
                        <div class="col-sm-5">
                        <label>نام دوره</label>
                        <input class="form-control input-sm" type="text" placeholder="نام دوره" name="courseName" value="@courseName" />
                        </div>
                        <div class="col-sm-5">
                        <label>گروه اصلی</label>
                        <select class="form-control" name="groupId" id="groupId" asp-items="@ViewData["Groups"] as SelectList"></select>
                        </div>
                        <div class="col-sm-5">
                        <label>گروه فرعی</label>
                        <select class="form-control" name="subGroupId" id="subGroupId" asp-items="@ViewData["SubGroups"] as SelectList"></select>
                        </div>
                        <div class="col-sm-2">
                        <button type="submit" class="btn btn-primary btn-sm">جستجو</button>
                        <a asp-page="Index" class="btn btn-default btn-sm">خالی</a>
                        </div>
                        <div class="col-sm-5"></div>
                        </div>
                        </form>*@
                        <div style="margin: 10px;">
                            <a href="/Admin/Courses/CreateEpisode/?courseId=@Model.AdminEpisodeListViewModel.CourseId" class="btn btn-success btn-outline btn-sm">افزودن بخش جدید</a>
                        </div>
                        <table class="table table-striped table-bordered table-hover dataTable no-footer" id="dataTables-example" aria-describedby="dataTables-example_info">
                            <thead>
                                <tr role="row">
                                    <th class="sorting_asc" tabindex="0" aria-controls="dataTables-example" rowspan="1" colspan="1" aria-sort="ascending" aria-label="Rendering engine: activate to sort column ascending">عنوان بخش</th>
                                    <th class="sorting" tabindex="0" aria-controls="dataTables-example" rowspan="1" colspan="1" aria-label="Browser: activate to sort column ascending">مدت زمان</th>
                                    <th class="sorting" tabindex="0" aria-controls="dataTables-example" rowspan="1" colspan="1" aria-label="Platform(s): activate to sort column ascending">رایگان</th>
                                    <th class="sorting" tabindex="0" aria-controls="dataTables-example" rowspan="1" colspan="1" aria-label="CSS grade: activate to sort column ascending">عملیات</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var episode in Model.AdminEpisodeListViewModel.EpisodeList)
                                {
                                    <tr>
                                        <td>@episode.CourseEpisodeTitle</td>
                                        <td>@episode.EpisodeTime</td>
                                        <td>
                                            @if (episode.IsFree)
                                            {
                                                <span class="text-primary">رایگان</span>
                                            }
                                            else
                                            {
                                                <span>غیر رایگان</span>
                                            }
                                        </td>
                                        <td>
                                            <a @*href="/Admin/Courses/EditCourse/?courseId=@course.CourseId"*@ class="btn btn-warning btn-sm">ویرایش</a>
                                            <a @*href="/Admin/Courses/DeleteCourse/?courseId=@course.CourseId"*@ class="btn btn-danger btn-sm">حذف</a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                        <div class="row">
                            <div class="col-sm-6">
                                <div class="dataTables_paginate paging_simple_numbers" id="dataTables-example_paginate">
                                    @*<ul class="pagination">
                                        @for (int i = 1; i <= Model.AdminCourseListViewModel.PagesCount; i++)
                                        {
                                            <li class="paginate_button @((Model.AdminCourseListViewModel.CurrentPage == i)? "active": "")" aria-controls="dataTables-example" tabindex="0">
                                                <a href="/Admin/Courses/?courseName=@courseName&groupId=@groupId&subGroupId=@subGroupId&pageId=@i">@i</a>
                                            </li>
                                        }
                                    </ul>*@
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <!-- /.table-responsive -->
            </div>
            <!-- /.panel-body -->
        </div>
        <!-- /.panel -->
    </div>
    <!-- /.col-lg-12 -->
</div>